"""Currency constants for D&D."""

from enum import Enum

class Currency(Enum):
    """Enum representing types of currency in D&D."""
    COPPER = "copper"
    SILVER = "silver"
    GOLD = "gold"
    PLATINUM = "platinum"
